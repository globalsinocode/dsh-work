# A1：输入附件来源权限修复

基线：3015d241。新增 `review-a1.integration.test.ts`，已接入 `test:review:integration` 与 CI。

修复前：在未修复的 Content Service 上执行同一个 HTTP 回归用例，已移出团队的员工向个人 Session 提交旧团队附件，实际返回 202（预期 403），测试失败且测试 Worker 收到调度。

修复后：5/5 专项通过；相邻文件解析、团队共享文件与文件版本套件 35/35 通过；服务器 TypeScript 与变更文件 ESLint 通过。验证环境：Node 22.19.0、真实 PostgreSQL 17.11、独立一次性测试库、受控测试身份。没有调用真实模型或企业身份服务。

实现：下载复核完整的当前身份/空间权限；挂载在任何正文读取之前检查同空间、会话作者、当前来源权限与解析状态；新增无正文读取的固定输入复核接口供 A2 出队检查。相同个人空间跨会话复用保留；跨空间隐式引用拒绝；归档后的当前成员只读下载保留。保留既有非枚举错误文案，基础设施错误不冒充权限拒绝。

迁移：无数据库迁移，不改写已有文件/会话/空间标识或快照。跨空间行为收紧需在升级说明中公布；不得保留绕过来源校验的兼容开关。

未验收：真实 DSH 读取、目标服务器文件扫描/数据出口；这部分不能由本次 HTTP 与 PostgreSQL 绿灯替代。
