# D12 与整批集成证据

## 实现

全局输入不再依赖空间列表、没有首团队回退；显式团队入口保持锁定。默认个人空间解析先查当前用户/租户状态，既存空间不能让停用账号再次解析成功。个人旧链接必须先授权。原 admin `requireSession` 专用分支未修改，也未引入 `findSessionRow` 替换它。

历史/文件共用分页请求生命周期；Store、对话草稿、引用和在途结果随账号/上下文切换失效，迟到回复不能重新污染新账号。主导航只显示新对话、团队空间和自动任务；历史对话、我的文件及旧 Skill 路由保留上下文入口和选择能力。界面统一“移除”，声明非物理删除。

## RED/GREEN

- D12 前端 RED：4 个既有基线断言失败（全局空间回退/伪造 payload、锁定团队、上下文残留引用、无权引用错误）；实现后 personal-start 4/4。服务端默认解析 RED：4 条中 1 条失败，停用用户仍能取得已有个人空间；修复后 4/4。
- 移除会话的单连接池回归：事务中借用连接池导致等待卡住已实际复现；全部检查留在原事务连接后通过。旧 m3 精确响应断言增加两个已公开的非物理删除语义字段，保留停止在途任务和禁止恢复的断言。
- 整批补充：同毫秒内三条会话/文件的微秒游标各有实际红灯；保留数据库微秒并以 text→timestamptz 绑定后均通过。
- B3 6/6，B4 7/7，B5 6/6，D12 默认解析与 HTTP 闭环 6/6：聚合 **25/25**，真实一次性 PostgreSQL。闭环 Runtime 明确为合成，不是真实 DSH。
- 员工端 **289/289**，管理端 **55/55**；旧个人卡片/选择器的体验断言按已确认 D12 修改，团队归档/拒绝与错误断言保留。新增页面/旧链接/Store 隔离 9/9 已包含在员工端总数，不重复计数。
- 上批回归：review unit 65/65、review integration 14/14、batch02 unit 11/11、batch02 integration 12/12、8b unit 20/20、8b integration 7/7。安装 11 通过、1 条真实 DSH 跳过；团队会话/文件/版本/归档 59/59、撤权 29/29、身份集成 5/5，admin 专用会话与 m3 编排 16/16。
- 服务器、员工端、管理端类型检查与构建通过。构建仍有大 chunk 提示，不修改阈值掩盖。

## 浏览器与环境限制

先启动独立 PostgreSQL/HTTP/Vite 进行真实浏览器预演，Chromium 管理策略拒绝 localhost 导航；随后显式运行 P1 Playwright 首例，API fixture 准备成功，首个 `page.goto` 同样返回 `ERR_BLOCKED_BY_ADMINISTRATOR`，其余 3 条未执行。不是业务断言通过，也不是真实 DSH/OIDC 验收。保留完整阻塞日志和测试 trace。

已提供 2 条新 P0 和 4 条 P1 spec，接入 CI，未跳过这些断言。受当前策略影响，本轮不能声称 Spec→Code→Verify→Test→Green 的浏览器阶段已经跑绿；本地/CI 必须继续执行它们。组件使用 happy-dom，也不冒充 Chromium。

pnpm 入口在本环境不可用，实际以 Node 22.19.0 运行服务器 package.json 中同一脚本串，以及 Vitest/vue-tsc/Vite/tsc 原命令。没有宣称顶层 `pnpm ci:check` 或远程 CI 通过。PostgreSQL 17.11；所有数据库均为测试专用可丢弃实例。真实 DSH/OIDC、企业扫描、Mac mini 和用户的 TW-10/AG-03 合并主线未运行。
