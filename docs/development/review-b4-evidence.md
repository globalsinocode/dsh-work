# B4 我的文件：实现与证据

- 复用 PostgresContentService 与既有 file_objects/file_extractions/Artifact 关系，新增统一个人资源分页。按底层对象去重，成果关系优先于附件分类；来源由服务端计算。GET /files 与 GET /files/:id 不接受他人身份或团队范围。
- POST /files 服务端解析默认个人 Workspace；下载复用原 readFile 授权；DELETE /files/:id 仅逻辑移除，原始文件/解析/历史 Run 引用不删除。旧个人文件摘要及个人成果列表不再重新显示被移除对象。
- RED：6 条真实 HTTP/PG 回归在旧代码失败（路由不存在，404）。GREEN：6/6；A1、文件解析、团队共享文件及版本相邻 40/40；服务端/员工端类型检查、定向 ESLint 通过。
- 前端 /files 提供来源筛选、名称搜索、分页、上传、处理状态、下载、移除及引用入口。引用的服务端解析与统一导航在 D12 完成；整批浏览器证据随 D12 留存。
- 无新增迁移，复用 0024 的逻辑移除字段与既有索引；不改变任何对象的 Workspace/Session 归属。文件解析失败但通过安全检查的原件可下载，不可引用；未解析成果不伪装成可引用输入。
- 真实 DSH/OIDC、企业级扫描、真实生成成果质量未运行。集成中的成果关系明确为合成 fixture，不是假冒模型输出。
